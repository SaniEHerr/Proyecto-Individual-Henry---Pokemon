import { ContainerLoader, StylesLoader } from "./styled.components";

const Loader = () => {
  return (
    <ContainerLoader>
      <StylesLoader />
    </ContainerLoader>
  );
};

export default Loader;